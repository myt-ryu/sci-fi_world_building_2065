# Server-side AI settings (used in /api/chat on Vercel)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# Optional: local development fallback (browser-side Gemini)
# Used only when `npm run dev` and VITE_GEMINI_API_KEY is set.
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.5-flash
